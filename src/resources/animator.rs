pub use bevy::prelude::*;

use super::Reality;

pub struct Animator {

}

impl Animator {
    pub fn init() -> Animator {
        Animator {

        }
    }
    pub fn system_player_animator(
        selfs: ResMut<Reality>,
    ) {
        
    }
}
